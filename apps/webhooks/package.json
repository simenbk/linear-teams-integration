{
  "name": "@linear-teams/webhooks",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/src/functions/*.js",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build --watch",
    "start": "func start",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "test": "vitest run",
    "test:unit": "vitest run",
    "test:watch": "vitest watch"
  },
  "dependencies": {
    "@azure/functions": "^4.4.0",
    "@linear-teams/shared": "*",
    "@linear-teams/linear-client": "*",
    "@linear-teams/db": "*",
    "@linear-teams/queue": "*"
  },
  "devDependencies": {
    "azure-functions-core-tools": "^4.0.0",
    "typescript": "^5.4.0"
  }
}
